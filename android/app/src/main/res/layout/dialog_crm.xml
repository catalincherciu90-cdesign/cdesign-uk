<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <EditText android:id="@+id/client" style="@style/Field" android:hint="Client name *" android:inputType="textCapWords" />
        <EditText android:id="@+id/proiect" style="@style/Field" android:hint="Project" android:inputType="textCapSentences" />
        <EditText android:id="@+id/valoare" style="@style/Field" android:hint="Value (e.g. £900)" android:inputType="text" />
        <EditText android:id="@+id/termen" style="@style/Field" android:hint="Deadline (e.g. 20 Oct)" android:inputType="text" />
        <EditText android:id="@+id/status" style="@style/Field" android:hint="Status (e.g. lead, negotiating, won)" android:inputType="text" />
        <EditText android:id="@+id/note" style="@style/Field" android:hint="Notes" android:inputType="textCapSentences|textMultiLine" android:minLines="2" android:gravity="top" />
    </LinearLayout>
</ScrollView>
